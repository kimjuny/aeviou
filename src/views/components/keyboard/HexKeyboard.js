import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles/HexKeyboard';

const HexKeyboard = () => (
  <View style={styles['hex-keyboard']}>
    <Text>HexKeyboard</Text>
  </View>
);

export default HexKeyboard;
